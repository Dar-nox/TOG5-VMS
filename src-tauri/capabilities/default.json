{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell only opens a window, so it needs nothing beyond the core defaults.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
