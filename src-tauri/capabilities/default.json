{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for TOG 5 VMS. Just a window: the opener permissions went with the file browser, which went with the local photo folders.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
