export function App() {
  return (
    <main className="app-shell">
      <section className="intro-panel" aria-labelledby="app-title">
        <p className="eyebrow">Local desktop workspace</p>
        <h1 id="app-title">TOG 5 VMS</h1>
        <p className="lede">
          Vehicle maintenance, fuel, alerts, reports, documents, and backups will be built in
          focused phases from the local-first specification.
        </p>
      </section>
    </main>
  );
}
